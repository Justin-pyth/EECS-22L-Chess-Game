#include "Moves.h"

void getMoves(int* moveCount)
{
    ;
}

struct move* getLegalMoves(struct piece p, int* moveCount)
{
    ;
}

bool isLegalMove(struct move moveMaid, const struct gameState* gs)
{
    ;
}